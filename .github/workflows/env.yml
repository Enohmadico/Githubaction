name: custom env vars 
 on:
   workflow_dispatch
   push:
     branches:
         -main 
env:
    weekday : friday
    name : edwin 
    
jobs:
 mainjob: 
     name: print env 
     runs-on: windows-latest

     steps:
      - name : print env 
        run : |
         write-host "${{en.weekdat}}"
         write-host "${{en.name}}"
    
   